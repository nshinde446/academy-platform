fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
sqlalchemy[asyncio]>=2.0.25,<3.0.0
asyncpg>=0.29.0,<1.0.0
alembic>=1.13.0,<2.0.0
pydantic-settings>=2.1.0,<3.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=4.0.1,<4.1.0
redis>=5.0.0,<6.0.0
celery>=5.3.0,<6.0.0
httpx>=0.27.0,<1.0.0
email-validator>=2.1.0,<3.0.0
aiosqlite>=0.19.0,<1.0.0
openpyxl>=3.1.0,<4.0.0
jinja2>=3.1.0,<4.0.0
python-multipart>=0.0.9,<1.0.0
# Materials ingest pipeline: PDF rendering + Gemini Vision extraction.
# Missing these from the prod image made ingest fail with
# "No module named 'fitz'".
pymupdf>=1.27.0,<2.0.0
google-genai>=2.0.0,<3.0.0
# Paper PDF generation (Tier 14): KaTeX in a headless Chromium so output
# matches the Question Bank preview byte-for-byte (no LaTeX edge cases).
# Chromium itself is installed via `playwright install` in the Dockerfile.
playwright>=1.50.0,<2.0.0
# matplotlib remains for the legacy mathtext-based pdf_service path,
# kept as an emergency fallback. Can be dropped once the browser route
# is fully bedded in.
matplotlib>=3.8.0,<4.0.0
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
